<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ .Title }}</title>
</head>
<body>
    <h1>{{ .Name }}</h1>
    <p>Address:</p>
    <p>{{ .Address.Street }}</p>
    <p>{{ .Address.City }}</p>
    <p>{{ .Address.Province }}</p>
</body>
</html>
